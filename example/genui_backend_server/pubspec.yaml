name: genui_backend_server
description: Backend-mode Genkit server example for the Flutter hybrid GenUI app.
publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  genkit: ^0.13.2
  genkit_llamadart: ^1.2.0
  genkit_shelf: ^0.1.2
  path: ^1.8.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
